# 本地开发环境变量示例（请复制为 .env 并按需修改）

# 运行环境
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# 数据库（Prisma 使用 DATABASE_URL）
# MySQL 示例：mysql://USER:PASS@HOST:3306/DB
# PostgreSQL 示例：postgresql://USER:PASS@HOST:5432/DB?schema=public
DATABASE_URL=mysql://ml:ml@localhost:3306/moonlens

# Redis（缓存/队列）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT 配置
JWT_SECRET=请替换为随机高强度字符串
JWT_EXPIRES_IN=7d

# GitLab 基础配置
GITLAB_BASE_URL=https://gitlab.com

# 加密密钥（必须）用于安全存储 GitLab 令牌
# 生成：openssl rand -base64 32
GITLAB_ENC_KEY=

# Webhook 回调对外基地址（可选，用于 GitLab 回调你的后端）
# 例如本地通过 ngrok 提供外网地址，如 https://xxxx.ngrok.io
PUBLIC_BASE_URL=

# Webhook 验证密钥（可选，若不设置，系统会在创建 Hook 时生成）
GITLAB_WEBHOOK_SECRET=

# AI 相关可留空（当前阶段不启用）
AI_SERVICE_URL=
AI_API_KEY=
AI_MODEL=

